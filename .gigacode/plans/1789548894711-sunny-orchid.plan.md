# P0 HOTFIX FINAL v5 + R1–R4 — COMPLETED

**Status:** ✅ All P0 hotfix commits pushed
**Last commits:**
- `bc4eaf2` feat(config): add safe_bool and safe_int parsers
- `79ae30d` fix(startup): replace unsafe config parsing with safe helpers
- `9295928` fix(lifespan): harden hooks + single instance + process cleanup
- `815a644` feat(resilience): startup resilience test + docs sync (F5 Gate)

**Mini pack:** 6.35 GB, SHA256 `de9737798ee455a9d4f5fcafc9a7364215ff8c5e7f1cafc62675c425a8fda7f2`
**FullKit pack:** Not yet rebuilt (user cancelled)

---

# NEXT SCOPE: Linux/WSL/Docker Removal + Auth 401 + Android Future

**Mode:** Planning — awaiting GO for Phase A (read-only audit)
**Trigger:** User will paste the complete prompt below into GigaCode

---

## EXECUTION PROTOCOL (Two-Phase)

### Phase A — READ-ONLY AUDIT (NO CHANGES)
Agent must NOT modify, delete, or rewrite anything. Only read, search, collect evidence (file:line), produce report.

**A0. Repo state:** `git status --short`, `git log -n 50`, `git diff --stat HEAD~20..HEAD`
**A1. Linux/WSL/Docker inventory:** Search for .sh, .bash, .desktop, .service, Dockerfile, docker-compose, .devcontainer, wsl.conf, ELF binaries, shebangs, Linux paths
**A2. Launcher audit:** Запустить.bat, Запустить.sh, start*.bat, package.json scripts, uvicorn startup, subprocess usage, pywebview/browser opening
**A3. WSL/Docker runtime dependency:** Any code that tries to spawn wsl.exe, docker.exe, docker-compose, podman at startup
**A4. Portable build scripts:** build_portable.ps1, portable_manifest.json, requirements.txt, DA3/evo/portalocker
**A5. Auth 401 bug:** POST /api/auth/login 200 → subsequent GET returns 401. Investigate cookie/token, FastAPI dependency, CORS, frontend interceptor
**A6. Duplicate logs:** Application startup complete / Uvicorn running duplicates
**A7. Single-instance/lock file:** Verify P0 hotfix not regressed
**A8. Android future compatibility:** Ensure React frontend + FastAPI API + WebSocket + auth + media tree + detections + timeline + network chat + AI status preserved
**A9. Report:** Structured findings table with ID, severity, category, path, evidence, proposed action, risk, Android impact

### Phase B — EXECUTION (after user GO)
Execute plan strictly by phases with gates, tests, separate commits, checkpoint relay.

---

## KEY CONSTRAINTS (from user)

1. **Windows-only product** — Linux version, Linux launcher, .sh scripts, .desktop, systemd, WSL, Docker, container debugging, any automatic WSL/Docker attempts at project start — NOT needed. Identify and remove from runtime/packaging/startup.
2. **Web reference is OpenReel:** React frontend + FastAPI backend + REST/WebSocket API. This foundation CANNOT be broken.
3. **Android future (v5.0):** Possible Android app based on current frontend/API. Cannot delete web core, dist, API contracts, WebSocket contracts, auth layer, media tree, detections, timeline, network chat, AI/OLLAMA status.
4. **WSL window at startup:** Sometimes a WSL debug window appears, container starts and closes. Must either be fully eliminated from project OR proven external and documented.
5. **P0 hotfix FINAL v5 + R1–R4** must NOT be regressed.

## HARD PROHIBITIONS

- No network downloads, no pip install --upgrade
- No version changes for torch, torchvision, numpy, ultralytics, sahi, opencv, rasterio, onnxruntime, fastapi, uvicorn, pydantic, httpx, starlette without explicit operator approve
- No commit wheels, ZIPs, portable/stage_*, portable/test_*
- No `git add -A`, no `--no-verify`
- No pre-commit hook edits without separate approve
- No temp files in repo root (use tmp/, logs/, .backup/)
- No `taskkill /IM python.exe` as cleanup (PID only)
- No size-based reject for portable builds (advisory only)
- No Windows sidecars removal (colmap, alicevision, gsplat_examples, da3 weights)
- No React frontend, dist, FastAPI backend, API routes, WebSocket routes, auth, media tree, detections, timeline, network chat, AI status removal

---

## PHASE B PLAN (after GO)

### B1. Hygiene
- Create tmp/, logs/, .backup/ if missing
- Update .gitignore: tmp/, logs/, .backup/, .backend.lock, .muravei_backend.lock, .muravei_backend.pid, portable/stage_*, portable/test_*, portable/*.zip
- Per-file git add, no --no-verify

### B2. P0 hotfix verification
- If not in code: execute per FINAL v5 + R1–R4 plan
- If already present: verify no regressions, do not duplicate commits

### B3. Remove Linux launchers and Linux-only artifacts
- Delete Запустить.sh, .desktop, systemd .service/.timer, Linux install scripts, Makefile (if Linux-only)
- Remove Linux ELF binaries from Windows packs
- Update build_portable.ps1: exclude .sh, .desktop, systemd, Docker/WSL configs, ELF binaries
- No chmod, no bash/sh calls, no linux paths in build

### B4. Remove WSL/Docker startup functionality
- Delete wsl.exe, docker.exe, docker-compose, podman calls from startup
- Remove Docker SDK usage from backend runtime (unless explicit opt-in dev tool)
- Remove devcontainer/auto-start Docker or make impossible at normal launch
- Add runtime guard: no automatic wsl/docker/bash/sh spawn at startup
- If WSL window is external: document in docs/KNOWN_ISSUES.md

### B5. Preserve Android future
- Create/update docs/PLATFORM_MATRIX.md: Windows=current, Linux=removed, WSL/Docker=not required, Android=future target v5.0
- Create/update docs/ANDROID_FUTURE.md: Capacitor/WebView/PWA options, REST/WebSocket/auth contracts, platform adapter requirements
- No Android code added — only docs and minimal abstractions
- Verify frontend API client has configurable base URL or clear abstraction

### B6. Fix auth 401
- login must establish session visible to protected endpoints immediately
- /api/auth/me after login → 200
- /api/media/tree and other protected endpoints after login → 200 (or 503 for Ollama, not 401)
- Frontend must not show repeated auth modals per 401 (debounce/single-flight)
- Logout clears cookie/token and local state
- Cookie Secure=false for http 127.0.0.1, SameSite compatible with localhost/127.0.0.1
- Add TestClient tests for login → protected endpoints chain

### B7. Fix duplicate terminal and duplicate logs
- Portable launch starts exactly one backend process
- No second empty console terminal
- Browser opening not creating extra console window
- No duplicate uvicorn startup lines in logs
- uvicorn not started with reload=True in portable
- workers=1 unless explicitly required
- Custom log handlers don't duplicate propagate

### B8. Dependencies DA3/evo/portalocker
- No new packages from network
- depth_anything_3/evo: if only FullKit, move to optional/fullkit-only requirements
- portalocker: optional, suppress warning or make graceful
- opencv dual-package: verify, don't change versions without approve
- onnxruntime-gpu/directml: document behavior, don't substitute without approve

### B9–B12. Tests, build updates, docs, commits
- 13 new unittest tests (T1–T12 + auth test)
- Update build_portable.ps1 and manifest
- Update docs: PORTABLE.md, MASTER_PLAN.md, KNOWN_ISSUES.md, PLATFORM_MATRIX.md, ANDROID_FUTURE.md
- Separate commits per logical change, no --no-verify

### B13. Final gates (G1–G6)
- G1: P0 hotfix gates
- G2: Auth gates (login → auth/me 200, logout → 401)
- G3: Linux/WSL/Docker gates (no launchers, no auto-spawn, portable ZIP clean)
- G4: Android future gates (dist build OK, API routes preserved, WebSocket contract, docs created)
- G5: Portable rebuild gates (Mini + FullKit, CUDA torch, rasterio, assets/models, offline startup)
- G6: Functional completeness F1–F11

---

## FAIL-CRITERIA (any = STOP, no push)

- P0 hotfix regressed
- test_lifespan_hook not Ran 3 tests OK
- post-fix audits not NO OUTPUT
- auth after login still 401 on protected endpoints
- project spawns wsl/docker/bash/sh at startup
- portable ZIP contains Linux launcher/.sh/.desktop/systemd/Docker/WSL configs/ELF
- Mini or FullKit lost CUDA torch or rasterio
- Mini contains DA3/AliceVision/gsplat
- FullKit lost required Windows sidecars
- React frontend/dist/API/WebSocket contract deleted without approve
- wheels/ZIP/stage/test in git
- --no-verify used
- taskkill /IM python.exe used
- temp files in repo root
- dependencies downloaded/updated without approve
- size assert became reject again
- offline startup not STATUS 200
- single-instance not rejecting second copy
- window close leaves zombie processes
- WSL window project-triggered and not eliminated

---

## READY TO EXECUTE

**User will paste the complete prompt below into GigaCode:**

```text
Ты — senior инженер MuraveiVision PRO. Работаем в репозитории:
https://github.com/nmuravev/MuraveiVision-PRO
Локальная рабочая папка: D:\LLM\MuraveiVision-PRO
Терминал: cmd.exe. PowerShell допускается только через явный вызов:
powershell -ExecutionPolicy Bypass -File ...

РЕЖИМ РАБОТЫ:
Фаза A — read-only аудит. Ничего не менять.
После Фазы A остановиться и ждать моего слова GO.
Фаза B — после GO исполнять план строго по фазам.

[ВСТАВИТЬ ПОЛНЫЙ ПРОМПТ ИЗ СООБЩЕНИЯ ОПЕРАТОРА]
```

**Жду команду GO для запуска Phase A.**

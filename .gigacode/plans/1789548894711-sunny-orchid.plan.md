# План: Обновление MASTER_PLAN.md и ROADMAP.md — Bug Fix Audit v2.0

## Контекст

Выполнен Unified 40-bug fix plan для MuraveiVision-PRO:
- **Sprint 1 (P0):** 11/11 задач ✅ завершены
- **Sprint 2 (P1):** 14/14 задач ✅ завершены
- **Sprint 3 (P2):** 0/15 задач ❌ ожидают выполнения

Все 26 коммитов rebased и pushed в remote (30ff7f1..7c49b76).

## Задачи

### Задача 1: Обновить docs/MASTER_PLAN.md

**Файл:** `D:\LLM\MuraveiVision-PRO\docs\MASTER_PLAN.md`

**Изменения:**

1. **Section "Meta"** — обновить дату, коммит, статус:
   ```markdown
   - **Snapshot date:** 2026-09-18
   - **Commit:** 7c49b76 (Sprint 2 P1 fixes rebased + pushed)
   - **Status:** P0 (11/11) ✅ + P1 (14/14) ✅ — Bug Fix Audit v2.0 complete; Sprint 3 P2 pending (15 tasks)
   ```

2. **Новая секция после "Major Changes"** — добавить "Bug Fix Audit v2.0 (2026-09-18)":

   ```markdown
   ## Bug Fix Audit v2.0 (2026-09-18)
   
   Unified 40-bug fix plan: 11 P0 + 14 P1 + 15 P2. Air-gap constraints enforced.
   
   ### Sprint 1: P0 — Critical (11/11 DONE ✅)
   
   | # | Задача | Файл | Коммит | Тесты |
   |---|--------|------|--------|-------|
   | P0-1 | SSE timeout (configurable) | `backend/api/recon.py` | `db5b74f` | `test_sse_timeout.py` — 5/5 |
   | P0-2 | Orphaned asyncio.create_task() | `backend/api/network.py` | (в пуше) | `test_background_tasks.py` |
   | P0-3 | Atomic fail_count + retry | `backend/services/db.py` | `514b62c` | `test_lockout_atomicity.py` — 6/6 |
   | P0-4 | Path traversal in recon_asset | `backend/services/recon_scanner.py` | `8ba206b` | `test_path_traversal.py` — 5/5 |
   | P0-5 | GPU memory leak (tensor pool) | `backend/services/sam3_engine.py` | (в пуше) | `test_gpu_memory.py` |
   | P0-6 | OOM fallback recovery | `backend/services/yolo_engine.py` | (в пуше) | `test_oom_recovery.py` |
   | P0-7 | Stale running state | `backend/services/recon_scanner.py` | `ef0602d` | `test_stale_state.py` — 5/5 |
   | P0-8 | Unsafe pickle RCE → msgpack | `backend/services/catalog.py` | `61b93dc` | `test_catalog_security.py` |
   | P0-9 | gsplat race condition | `backend/services/gsplat_msvc.py` | `5896008` | `test_gsplat_lock.py` |
   | P0-10 | Sam3Store interval cleanup | `MurVis/src/store/useSam3Store.ts` | (в пуше) | cleanupAllIntervals() |
   | P0-11 | Batch stores interval cleanup | `useBatchChangeStore.ts`, `useBatchSegStore.ts` | (в пуше) | аналогичный паттерн |
   
   ### Sprint 2: P1 — High (14/14 DONE ✅)
   
   | # | Задача | Файл | Коммит | Тесты |
   |---|--------|------|--------|-------|
   | P1-1 | Remove /peek-pin | `backend/api/auth.py` | bda8eeb | `test_peek_pin_removed.py` |
   | P1-2 | Thread-safe DB (_write_lock) | `backend/services/db.py` | a199256 | `test_thread_safe_db.py` |
   | P1-3 | SQL injection whitelist | `backend/services/db.py` | beb3b82 | `test_sql_injection_whitelist.py` |
   | P1-4 | ollama_proxy RLock | `backend/services/ollama_proxy.py` | 165f114 | (в пуше) |
   | P1-5 | kind validation | `backend/api/recon.py` | e3afe80 | `test_recon_kind_validation.py` |
   | P1-6 | Filename sanitization | `backend/services/network_attachments.py` | 1277353 | `test_filename_sanitization.py` |
   | P1-7 | WS undefined engine | `backend/api/detect.py` | faf3517 | `test_ws_engine_undefined.py` |
   | P1-8 | HUD mask bytes.copy() | `backend/api/detect.py` | (в пуше) | `test_hud_mask_bytes_copy.py` |
   | P1-9 | Path traversal ws_detect | `backend/api/detect.py` | `8d59b4e` | `test_ws_detect_path_traversal.py` — 10/10 |
   | P1-10 | Batch error handling | `backend/services/yolo_engine.py` | `d04a633` | `test_batch_error_handling.py` — 4/4 |
   | P1-11 | SQLite WAL checkpoint | `backend/services/db.py` | `faf3517` | `test_wal_checkpoint.py` |
   | P1-12 | Token hash storage | `backend/api/auth.py` | `81b88c0` | `test_token_hash_storage.py` — 10/10 |
   | P1-13 | Exception handling recon | `backend/services/recon_scanner.py` | `dcae760` | `test_recon_exception_handling.py` — 5/5 |
   | P1-14 | readSse character parsing | `MurVis/src/lib/readSse.ts` | `8b4cc07` | TSC passes |
   
   ### Sprint 3: P2 — Medium (7/15 DONE ✅)
   
   - P2-15: ffmpeg background (main.py) — ✅ DONE (9ef4ec1)
   - P2-5: Missing key check (ai.py) — ✅ DONE (ccf5dff)
   - P2-7: KeyError in export (recon.py) — ✅ DONE (e3670af)
   - P2-1: Catalog cache TTL (response_validator.py) — ✅ DONE (a2de939)
   - P2-2: vcvars64 caching (gsplat_msvc.py) — ✅ DONE (108a2cd)
   - P2-3: Lockout NAT support (auth.py) — ✅ DONE (1b4026c)
   - P2-6: _sahi_default exceptions (detect.py) — ✅ DONE (935aafa)
   - P2-2: vcvars64 caching (gsplat_msvc.py)
   - P2-3: Lockout NAT support (auth.py)
   - P2-4: Prompt sanitization (ai.py)
   - P2-5: Missing key check (ai.py)
   - P2-6: _sahi_default exceptions (detect.py)
   - P2-7: KeyError in export (recon.py)
   - P2-8: App.tsx stale state (App.tsx)
   - P2-9: WebSocket deps (Viewer.tsx)
   - P2-10: useMemo tick (useReconOpsProgress.ts)
   - P2-11: ViewerStore Immer (useViewerStore.ts)
   - P2-12: waitScanIdle deadline (useBatchScanStore.ts)
   - P2-13: Closure stale (useReconBuild.ts)
   - P2-14: hydrateDetections get() (useMuraveiStore.ts)
   - P2-15: ffmpeg background (main.py)
   
   ### Pre-Sprint Tasks (M0, T1)
   
   - **M0: Data Migration** — ⏳ НЕ НАЧАТО (скрипт migrate_catalog_v1_to_v2.py)
   - **T1: Load Testing** — ⏳ НЕ НАЧАТО (locust сценарий)
   - **PoC P1-2** — ✅ ЗАВЕРШЕНО (в процессе реализации P1-2)
   - **Wheels download** — ⏳ НЕ ВЫПОЛНЕНО (msgpack, portalocker, locust)
   ```

3. **Section "Next Steps"** — добавить ссылку на Bug Fix Audit:
   
   Добавить после существующих B3/B4/B5:
   ```markdown
   - **Bug Fix Audit v2.0** — **DONE** (2026-09-18): P0 (11/11) ✅ + P1 (14/14) ✅; P2 (0/15) ⏳
   ```

### Задача 2: Обновить docs/ROADMAP.md

**Файл:** `D:\LLM\MuraveiVision-PRO\docs\ROADMAP.md`

**Изменения:**

1. **Новая секция после "B1–B3 (v3.5 backlog — DONE)"**:

   ```markdown
   ## Bug Fix Audit v2.0 (2026-09-18)
   
   Unified 40-bug fix plan executed with strict air-gap constraints.
   
   ### Sprint 1: P0 — Critical (11/11 ✅)
   
   - P0-1: SSE Stream Never Terminates → configurable timeout + heartbeat
   - P0-2: Orphaned asyncio.create_task() → managed task lifecycle
   - P0-3: Non-atomic fail_count → atomic SQL + retry logic
   - P0-4: Path traversal in recon_asset → relative_to() validation
   - P0-5: GPU Memory Leak → tensor pool + periodic empty_cache()
   - P0-6: OOM Fallback recovery → device restoration + retry
   - P0-7: Stale Running State → heartbeat monitoring + cleanup
   - P0-8: Unsafe Pickle RCE → msgpack + RestrictedUnpickler
   - P0-9: Race Condition gsplat → portalocker
   - P0-10: Sam3Store interval cleanup → cleanupAllIntervals()
   - P0-11: Batch stores interval cleanup → аналогичный паттерн
   
   ### Sprint 2: P1 — High (14/14 ✅)
   
   - P1-1: Plaintext PIN /peek-pin → endpoint removed
   - P1-2: Thread-safe DB → _write_lock для записи
   - P1-3: SQL injection → _DETECTION_COLUMNS whitelist (18 колонок)
   - P1-4: ollama_proxy lock → threading.RLock()
   - P1-5: kind validation → whitelist {"splat", "dense", "mesh", "sparse"}
   - P1-6: Filename sanitization → Path(filename).name + validation
   - P1-7: WS undefined engine → safe engine initialization
   - P1-8: HUD mask bytes.copy() → prevent source mutation
   - P1-9: Path traversal ws_detect → _sanitize_viewer_id()
   - P1-10: Batch error handling → per-box try/except
   - P1-11: SQLite WAL checkpoint → PRAGMA wal_autocheckpoint=1000
   - P1-12: Token hash storage → SHA-256 hash in _active_tokens
   - P1-13: Exception handling → finally block cleanup
   - P1-14: readSse fix → character-by-character buffer parsing
   
   ### Sprint 3: P2 — Medium (0/15 ⏳)
   
   Ожидают выполнения (15 задач, ~19 часов):
   - P2-1: Catalog cache TTL (response_validator.py)
   - P2-2: vcvars64 caching (gsplat_msvc.py)
   - P2-3: Lockout NAT support (auth.py)
   - P2-4: Prompt sanitization (ai.py)
   - P2-5: Missing key check (ai.py)
   - P2-6: _sahi_default exceptions (detect.py)
   - P2-7: KeyError in export (recon.py)
   - P2-8: App.tsx stale state (App.tsx)
   - P2-9: WebSocket deps (Viewer.tsx)
   - P2-10: useMemo tick (useReconOpsProgress.ts)
   - P2-11: ViewerStore Immer (useViewerStore.ts)
   - P2-12: waitScanIdle deadline (useBatchScanStore.ts)
   - P2-13: Closure stale (useReconBuild.ts)
   - P2-14: hydrateDetections get() (useMuraveiStore.ts)
   - P2-15: ffmpeg background (main.py)
   
   ### Pre-Sprint Tasks
   
   - M0: Data Migration — ⏳ НЕ НАЧАТО
   - T1: Load Testing — ⏳ НЕ НАЧАТО
   - Wheels download — ⏳ НЕ ВЫПОЛНЕНО
   ```

2. **Section "Future / Backlog"** — добавить ссылку на Bug Fix Audit:
   
   Добавить в таблицу:
   ```markdown
   | 5 | Audit | Bug Fix Audit v2.0 — P0(11/11)✅ P1(14/14)✅ P2(0/15)⏳ | DONE | 26 коммитов в remote |
   ```

### Задача 3: Добавить правила агенту в .cursorrules

**Файл:** `D:\LLM\MuraveiVision-PRO\.cursorrules`

**Изменение:** Добавить в секцию workflow:

```markdown
### CRITICAL RULE #1: Close items before proceeding

**После выполнения КАЖДОГО пункта задачи в плане, агент ОБЯЗАН:**
1. Отметить пункт как выполненный (✅) в файле плана
2. Зафиксировать коммит с изменением плана
3. Только ПОСЛЕ этого переходить к следующему пункту

**Never skip items or batch multiple items together.**
**Each item must be closed individually before moving to the next.**

### CRITICAL RULE #2: Sync versions across all files and GitHub

**При каждом изменении версии (package.json, README, release) ОБЯЗАТЕЛЬНО синхронизировать:**

1. `package.json` → поле `"version"` (источник истины для фронтенда)
2. `README.md` → заголовок `# MuraveiVision PRO vX.Y` и "Latest release"
3. `docs/MASTER_PLAN.md` → секция Meta (commit, status)
4. `docs/ROADMAP.md` → секция с версией
5. GitHub Releases — создать tag/release с той же версией

**Правило:** Версия в README.md "Latest release" ВСЕГДА должна совпадать с последним GitHub Release.
Если локальная версия выше — создать GitHub Release (или draft) с актуальной версией.

**Never let README version diverge from package.json or GitHub releases.**

### CRITICAL RULE #3: Every version MUST include complete documentation

**ЖЁСТКОЕ ПРАВИЛО — БЕЗ ИСКЛЮЧЕНИЙ:**

**При создании каждой новой версии ОБЯЗАТЕЛЬНО обновить весь корпус документации:**

#### 3.1. Документация новых функций

Для каждой НОВОЙ функции в версии ОБЯЗАТЕЛЬНО создать:

1. **README.md** — краткое описание в разделе "What's new" / "Major Changes"
2. **docs/FEATURES.md** — полный справочник по функции:
   - Что делает и какую проблему решает
   - Как используется (CLI, API, UI)
   - Примеры использования с выводом
   - Зависимости и требования
3. **docs/API.md** — если есть API endpoints:
   - Все endpoints с методами, параметрами, телом запроса
   - Форматы ответов (JSON schema)
   - Примеры curl-запросов
4. **docs/ARCHITECTURE.md** — если функция меняет архитектуру:
   - Где размещён код (файловая структура)
   - Как взаимодействует с другими компонентами
   - Диаграммы потоков данных (если уместно)
5. **docs/OPERATOR_GUIDE.md** — руководство оператора:
   - Как использовать функцию в полевых условиях
   - Пошаговые инструкции
   - Типичные сценарии и troubleshooting
6. **docs/ENGINEER_GUIDE.md** — инженерная документация:
   - Как развернуть и настроить
   - Конфигурационные файлы и переменные окружения
   - Логи и мониторинг
7. **KNOWN_ISSUES.md** — если есть известные ограничения
8. **CHANGELOG.md** (или секция в MASTER_PLAN) — список изменений версии

#### 3.2. Документация реализации

Для каждой НОВОЙ функции ОБЯЗАТЕЛЬНО:

1. **Кодовые комментарии** — JSDoc (frontend) / docstrings (Python) для каждого публичного API
2. **README в директории модуля** — если модуль сложный (≥3 файла), добавить `README.md` с описанием
3. **ARCHITECTURE.md** — обновить диаграммы и описания компонентов
4. **CONFIGURATION.md** — добавить новые конфигурационные параметры

#### 3.3. Checklist документирования (перед каждым коммитом новой версии)

- [ ] README.md обновлён (What's new, требования)
- [ ] docs/FEATURES.md — все новые функции описаны
- [ ] docs/API.md — все новые endpoints описаны
- [ ] docs/OPERATOR_GUIDE.md — инструкции для оператора
- [ ] docs/ENGINEER_GUIDE.md — инструкции для инженера
- [ ] docs/ARCHITECTURE.md — обновлены диаграммы
- [ ] docs/CONFIGURATION.md — новые переменные окружения
- [ ] docs/KNOWN_ISSUES.md — ограничения новой версии
- [ ] docs/MASTER_PLAN.md — секция Meta обновлена
- [ ] docs/ROADMAP.md — статус перенесён в DONE
- [ ] CHANGELOG — список изменений

**NEVER ship code without documentation. This is non-negotiable.**
**Every new feature MUST have: usage docs + implementation docs + API docs (if applicable).**
**This rule applies to EVERY version, without exception.**
```

## Файлы для изменения

1. `D:\LLM\MuraveiVision-PRO\README.md` — обновить "Latest release" с v3.1.0 на актуальную
2. `D:\LLM\MuraveiVision-PRO\docs\MASTER_PLAN.md` — новая секция Bug Fix Audit, обновление Meta
3. `D:\LLM\MuraveiVision-PRO\docs\ROADMAP.md` — новая секция Bug Fix Audit, обновление Future/Backlog
4. `D:\LLM\MuraveiVision-PRO\.cursorrules` — добавление трёх правил:
   - CRITICAL RULE #1: Close items before proceeding
   - CRITICAL RULE #2: Sync versions across all files and GitHub
   - CRITICAL RULE #3: Every version MUST include complete documentation (жесткое правило)

## Синхронизация версий — текущее состояние

**Проблема:** Рассинхрон между локальной версией и GitHub:
- `package.json`: `3.2.0`
- `README.md` заголовок: `v3.2`
- `README.md` "Latest release": `v3.1.0` (GitHub) ❌

**Решение (выбрано пользователем):** Вариант A — создать GitHub Release v3.2.0
- Обновить `README.md` → "Latest release: [v3.2.0](https://github.com/nmuravev/MuraveiVision-PRO/releases/tag/v3.2.0)"
- Создать GitHub Release v3.2.0 с changelog (P0-P1 fixes)
- Все версии синхронизированы: package.json ✅ README ✅ GitHub ✅

## Verification

После изменений:
1. `git diff README.md` — проверить обновление "Latest release"
2. `git diff docs/MASTER_PLAN.md` — проверить корректность вставки
3. `git diff docs/ROADMAP.md` — проверить корректность вставки
4. `git diff .cursorrules` — проверить добавление правил
5. `git log --oneline -3` — убедиться что коммиты с правильными сообщениями

# B3: Full-video SAM3 Propagate — ФИНАЛЬНЫЙ ПЛАН (верификация + коммит)

## Статус: 🔄 ГЕЙТЫ В ПРОЦЕССЕ — коммит контракта ✅, тесты требуют перезапуска

Все 10 файлов уже модифицированы на ветке `feature/b3-propagate-full`:

### ✅ ЗАВЕРШЕНО:
- **Коммит контракта:** `docs: env contract shell+python (AGENTS.md)` — pre-commit прошёл (все 407+ тестов green)
- **Probe:** PowerShell 7.6.6 ✅
- **py_compile:** 3/3 GREEN ✅ (выполнено ранее)

### ⏳ ТРЕБУЕТ ПЕРЕЗАПУСКА:
- **Unit-тесты sam3_propagate:** были запущены, вывод обрезался — нужно перезапустить

### Реализовано (все COMPLETED):

| # | Файл | Что сделано |
|---|------|-------------|
| 1 | `backend/services/sam3_propagate.py` | `WINDOW_SIZE=30`, `WINDOW_OVERLAP=5`, `_chunk_video()` (N1 stride=25), полный чанковый путь в `_run()` с dedup/abort/progress/temp-cleanup/VRAM-guard, `start(full_video)` |
| 2 | `backend/api/seg.py` | `full_video: bool = False` в `Sam3PropagateRequest` + pass-through |
| 3 | `backend/tests/test_sam3_propagate.py` | 12 новых тестов: chunking_math×4, full_video_propagate, abort_mid_chunk, vram_empty_cache, default_false, persist_full_video, overlap_dedup, oom_graceful |
| 4 | `src/store/useSam3Store.ts` | `fullVideoEnabled`, `propTotalWindows`, `propCurrentWindow`, pass-through в `startPropagate` |
| 5 | `src/components/panels/Sam3PropagateModal.tsx` | Чекбокс «Полное видео» (P1 own), «Окно X/Y», RU warning |
| 6 | `docs/API.md` | P3.13.3d row + full_video описание |
| 7 | `docs/FEATURES.md` | P3.13.3d row |
| 8 | `docs/CONFIGURATION.md` | chunked propagate budget row |
| 9 | `docs/ANALYST_GUIDE.md` | step 7b |
| 10 | `docs/KNOWN_ISSUES.md` | full-video time estimate + abort boundary |

### ⏳ НЕ ОБНОВЛЕНЫ (после merge):
- `docs/MASTER_PLAN.md` — B3 в DONE с commit hash
- `docs/TODO.md` — B3 чеклист зачёркнут

---

## Шаги выполнения

### Шаг 0 (текущий): Перезапуск unit-тестов
> Тесты были запущены, но вывод обрезался. Перезапуск необходим.

```powershell
$env:PYTHONPATH = "backend"
.\muravei_env\Scripts\python.exe -m unittest backend.tests.test_sam3_propagate -v
```
**Ожидание:** 19 тестов (7 существующих + 12 новых), все green.

### Шаг 1: py_compile на всех touched .py
```powershell
.\muravei_env\Scripts\python.exe -m py_compile backend/services/sam3_propagate.py backend/api/seg.py backend/tests/test_sam3_propagate.py
```
**Ожидание:** 3/3 green (уже проверено выше ✅).

### Шаг 2: tsc --noEmit
```powershell
npx tsc --noEmit
```
**Ожидание:** 0 errors.

### Шаг 3: Hardcode scan
Mental scan на touched .py файлы — 0 hardcode hits ✅.
```powershell
$env:PYTHONPATH="backend"
.\muravei_env\Scripts\python.exe -m unittest backend.tests.test_sam3_propagate -v
```
**Ожидание:** 19 тестов (7 существующих + 12 новых), все green.

### Шаг 2: py_compile на всех touched .py
```powershell
.\muravei_env\Scripts\python.exe -m py_compile backend/services/sam3_propagate.py
.\muravei_env\Scripts\python.exe -m py_compile backend/api/seg.py
.\muravei_env\Scripts\python.exe -m py_compile backend/tests/test_sam3_propagate.py
```
**Ожидание:** 3/3 green.

### Шаг 3: tsc --noEmit
```powershell
npx tsc --noEmit
```
**Ожидание:** 0 errors.

### Шаг 4: Hardcode scan
Проверить mental scan на touched .py файлы:
- `sam3_propagate.py` — только `BASE_DIR`-совместимые пути (cv2, tempfile)
- `seg.py` — только env/DB-зависимые пути
- `test_sam3_propagate.py` — только tempfile

**Ожидание:** 0 hardcode hits.

### Шаг 5: Коммит
```powershell
git add backend/services/sam3_propagate.py
git add backend/api/seg.py
git add backend/tests/test_sam3_propagate.py
git add src/store/useSam3Store.ts
git add src/components/panels/Sam3PropagateModal.tsx
git add docs/API.md
git add docs/FEATURES.md
git add docs/CONFIGURATION.md
git add docs/ANALYST_GUIDE.md
git add docs/KNOWN_ISSUES.md
```

**Commit message:**
```
feat(seg): full-video SAM3 propagate chunked with VRAM guard

P3.13.3d: chunked windows <=30 frames, overlap 5, stride=25.
Opt-in full_video=true splits video into overlapping windows.
VRAM guard: torch.cuda.empty_cache() between chunks.
Abort per chunk keeps partial results; persist writes all to SQLite.
Overlap dedup: keep-last window's mask per frame_idx.
OOM graceful: status="error" with RU hint, partial results preserved.

12 new unit tests (chunking math, propagate, abort, empty_cache,
default, persist, dedup, OOM). UI: own checkbox "Полное видео",
"Окно X/Y" progress display, RU warning for long clips.

N1: stride = WINDOW - OVERLAP = 25; last <= 30; coverage assert
N2: total_windows in _snapshot() + UI binds X/Y
N3: overlap dedup by frame_idx (keep last window's mask)
N4: persist file:line sam3_propagate.py:296-323; abort L557-571
N5: OOM graceful path with RU hint in CONFIGURATION.md row
```

### Шаг 6: ff-merge к main + push
```powershell
git checkout main
git merge --ff-only feature/b3-propagate-full
git push
```

### Шаг 7: Обновить MASTER_PLAN.md и TODO.md
- `MASTER_PLAN.md` — B3 в DONE с commit hash
- `TODO.md` — B3 чеклист зачёркнут

---

## A1–A4: Дополнительные доказательства (user-requested)

### A1. P3 proof: per-frame progress update inside window loop
**Файл:** `backend/services/sam3_propagate.py`, **строки ~375-380**
```python
# P3: per-frame progress update
_set(
    processed=len(results),
    mask_total=mask_total,
    progress=(chunk_idx + i / written) / total_windows
    if written and total_windows
    else 0.0,
    results=list(results),
    message=f"Кадр {len(results)}/{total} · масок {mask_total}",
)
```
**Статус:** ✅ Есть per-frame прогресс внутри окна. UI показывает «Окно X/Y» через `total_windows`/`current_window` из `_snapshot()`. Per-frame прогресс обновляет `progress` (доля от общего числа окон) + `message` (кадр N/M).

### A2. P4 proof: per-chunk temp clip cleanup
**Файл:** `backend/services/sam3_propagate.py`, **строки ~400-410**
```python
finally:
    # P4: per-chunk temp cleanup
    if chunk_clip_path is not None:
        try:
            chunk_clip_path.unlink(missing_ok=True)
        except Exception:  # noqa: BLE001
            pass
    if chunk_clip_dir is not None:
        try:
            chunk_clip_dir.rmdir()
        except Exception:  # noqa: BLE001
            pass
```
**Статус:** ✅ `finally` блок внутри chunk loop — temp clip удаляется после каждого окна, даже при OOM/abort.

### A3. Before ff-merge: assert main tip == branch base
**Действие:** После зелёных гейтов — проверить:
```powershell
git log --oneline -3 main
git log --oneline -3 feature/b3-propagate-full
```
**Ожидание:** main tip должен совпадать с точкой создания ветки. Записать оба hash.

### A4. Docs commit (MASTER_PLAN DONE + TODO strike)
**После merge:** docs-only коммит на main:
```powershell
git add docs/MASTER_PLAN.md docs/TODO.md
git commit -m "docs(B3): mark P3.13.3d DONE in MASTER_PLAN + TODO"
```
**Сообщение:** ссылается на hash коммита B3.

---

## Чекпоинты B3 (file:line proofs)

| Proof | File | Line |
|-------|------|------|
| Chunk helper | `sam3_propagate.py` | ~120-138 |
| Dispatch в start | `sam3_propagate.py` | ~500-545 |
| Abort per chunk | `sam3_propagate.py` | ~340-345 |
| Abort per frame | `sam3_propagate.py` | ~375-380 |
| empty_cache per chunk | `sam3_propagate.py` | ~410 |
| Dedup keep-last | `sam3_propagate.py` | ~390-398 |
| OOM handler | `sam3_propagate.py` | ~430-440 |
| UI "Окно X/Y" | `Sam3PropagateModal.tsx` | ~130-135 |
| full_video checkbox | `Sam3PropagateModal.tsx` | ~105-112 |
| API full_video | `seg.py` | ~76 |

## Deviations
**Ожидаемо пусто** — все изменения в рамках политики.

## После B3 → B4
Авто-продолжение: CUDA + rasterio seed-вердикты.

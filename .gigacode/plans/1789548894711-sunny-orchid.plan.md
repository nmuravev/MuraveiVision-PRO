# ML Audit Fixes — Обновлённый план (с учётом аудита от 2026-09-18)

**Ветка:** `fix/ml-audit-fixes`  
**Статус:** 🟢 **ВЫПОЛНЕНО** — все P0 задачи реализованы  
**Общая оценка:** 49 часов (P0: 24ч, P1: 25ч)  
**Аудит:** 🟡 7/10 → ✅ **Реализовано**  
**Реализовано:** P0-3, P0-5, P0-6, P0-1, P0-2, P0-4, P1-9, DB versioning, tests

---

## 📋 Сводка изменений после аудита

| Задача | Было (ч) | Стало (ч) | Изменения |
|--------|----------|-----------|-----------|
| P0-1: Excluded classes | 6 | **8** | +2ч: cache invalidation, INSERT OR IGNORE |
| P0-2: OOM graceful degradation | 3 | **4** | +1ч: обработка конкретных ошибок CUDA |
| P0-3: Ollama timeout | 2 | **2** | ✅ Без изменений |
| P0-4: Mutual exclusion | 6 | **10** | +4ч: новый сервис model_mutex.py, race conditions |
| P0-5: LRU cache | 2 | **2** | ✅ Без изменений |
| P0-6: Pin DA3 version | 1 | **1** | ✅ Без изменений |
| P1-7: Batch seg checkpoint | 8 | **12** | +4ч: миграции БД, resume logic |
| P1-8: GPU monitoring | 6 | **8** | +2ч: интеграция с status_snapshot |
| P1-9: Prompt sanitization | 2 | **2** | ✅ Без изменений |
| P1-10: Aliases в YAML | 4 | **4** | ✅ Без изменений (не обсуждался в аудите) |
| **ИТОГО** | **36** | **49** | **+36%** |

---

## 🆕 Новая задача: DB Schema Versioning — 2ч (добавлено после аудита)

**Проблема:** План не учитывает миграции БД для существующих установок.

**Файл:** `backend/services/db.py`

**Шаги:**
1. Добавить таблицу `schema_version`:
```sql
CREATE TABLE IF NOT EXISTS settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL
);
```

2. Добавить функции:
```python
def get_schema_version() -> int:
    conn = _connect()
    try:
        row = conn.execute(
            "SELECT value FROM settings WHERE key = 'schema_version'"
        ).fetchone()
        return int(row["value"]) if row else 0
    finally:
        conn.close()

def migrate_db() -> None:
    version = get_schema_version()
    conn = _connect()
    try:
        if version < 1:
            conn.execute("""CREATE TABLE IF NOT EXISTS excluded_classes (
                class_name TEXT PRIMARY KEY,
                added_at REAL NOT NULL DEFAULT strftime('%s', 'now')
            )""")
            # Seed 78 COCO classes с INSERT OR IGNORE
            conn.execute("INSERT INTO settings (key, value) VALUES ('schema_version', '1')")
        if version < 2:
            conn.execute("""CREATE TABLE IF NOT EXISTS batch_seg_jobs (
                task_id TEXT PRIMARY KEY,
                video_path TEXT NOT NULL,
                frame_step INTEGER NOT NULL DEFAULT 30,
                confidence REAL NOT NULL DEFAULT 0.25,
                status TEXT NOT NULL DEFAULT 'running',
                last_frame_idx INTEGER NOT NULL DEFAULT 0,
                total_frames INTEGER NOT NULL DEFAULT 0,
                results_json TEXT NOT NULL DEFAULT '[]',
                created_at REAL NOT NULL,
                updated_at REAL NOT NULL
            )""")
            conn.execute("UPDATE settings SET value = '2' WHERE key = 'schema_version'")
        conn.commit()
    finally:
        conn.close()
```

3. Вызвать `migrate_db()` при инициализации приложения (FastAPI startup event)

**Smoke test:**
```bash
# Первая инициализация
python -c "from services.db import migrate_db; migrate_db(); print('v1 OK')"
# Повторная инициализация — без ошибок
python -c "from services.db import migrate_db; migrate_db(); print('v2 OK')"
```

---

## P0 — Критические (Неделя 1, 24 часа)

### P0-1: Динамические excluded classes (без хардкода) — 8ч ⚠️ +2ч

**Проблема:** `_COCO_DROP` (78 классов) жёстко закодирован в `yolo_engine.py:67-133`. Оператор/инженер не может добавлять/удалять excluded classes без правки кода и рестарта.

**Требование:** Excluded classes редактируются через UI в работающем проекте.

**Файлы:** `db.py`, `classes.py`, `classes_api.py`, `yolo_engine.py`

#### Шаг 1: Таблица excluded_classes (2ч)
- Добавить в schema (через migration, см. выше)
- Seed 78 COCO-классов с `INSERT OR IGNORE` (не `INSERT`!)
- Функции `list_excluded_classes()`, `upsert_excluded_class()`, `delete_excluded_class()` в `db.py`

#### Шаг 2: Cache с инвалидацией (2ч)
```python
# В classes.py:
_excluded_cache: set[str] | None = None

def get_excluded_classes() -> set[str]:
    """Return set of excluded class names (lowercase, underscored)."""
    global _excluded_cache
    if _excluded_cache is not None:
        return _excluded_cache
    
    excluded: set[str] = set()
    try:
        from services.db import list_excluded_classes
        for row in list_excluded_classes():
            excluded.add(row["class_name"].lower().replace(" ", "_"))
    except Exception:
        pass
    
    _excluded_cache = excluded
    return excluded

def invalidate_excluded_cache() -> None:
    """Call after INSERT/DELETE on excluded_classes table."""
    global _excluded_cache
    _excluded_cache = None
```

#### Шаг 3: Интеграция с yolo_engine (2ч)
- Заменить `_COCO_DROP` на `get_excluded_classes()` в `yolo_engine.py`
- Вызывать `invalidate_excluded_cache()` после API mutations

#### Шаг 4: API endpoints (2ч)
- `GET /api/classes/excluded` — список
- `POST /api/classes/excluded` — добавить
- `DELETE /api/classes/excluded/{class_name}` — удалить

**Smoke test:**
```bash
# 1. Seed работает
python -c "from services.db import list_excluded_classes; print(f'{len(list_excluded_classes())} excluded')"

# 2. API добавляет класс
curl -X POST http://localhost:8000/api/classes/excluded -d '{"class_name":"test_excluded"}'

# 3. Cache инвалидируется
python -c "from services.classes import get_excluded_classes; assert 'test_excluded' in get_excluded_classes()"

# 4. YOLO игнорирует excluded класс
# (запустить inference с объектом test_excluded, проверить что не детектируется)
```

---

### P0-2: OOM graceful degradation — 4ч ⚠️ +1ч

**Проблема:** При CUDA OOM `torch.cuda.empty_cache()` НЕ вызывается перед retry на CPU.

**Файл:** `backend/services/yolo_engine.py`

#### Рекомендуемая реализация:
```python
# В методе _load_model() или predict():
try:
    self._model = YOLO(str(path))
except RuntimeError as exc:
    error_msg = str(exc).lower()
    if "cuda" in error_msg and ("out of memory" in error_msg or "memory" in error_msg):
        print(f"[YOLO] CUDA OOM during load → empty_cache + CPU fallback")
        try:
            import torch
            torch.cuda.empty_cache()
        except Exception:
            pass
        
        # Retry на CPU
        self._device = "cpu"
        self._device_backend = "cpu-fallback-oom"
        self._yolo_label = "CPU"
        self._degraded = True
        self._model = YOLO(str(path), device="cpu")
    else:
        raise
```

**Важно:** Обработать конкретные ошибки CUDA (не generic `Exception`):
- `RuntimeError: CUDA out of memory`
- `torch.cuda.OutOfMemoryError`

**Smoke test:**
```bash
python -c "from services.yolo_engine import YoloEngine; print('OK')"
# Проверить лог при загрузке на системе с малым VRAM
```

---

### P0-3: Rate limiting + timeout Ollama API — 2ч ✅

**Статус кода:** `timeout=gen_timeout` уже используется в `ollama_proxy.py:581`, `GENERATE_TIMEOUT = 60.0` (строка 32).

**Изменения:**
1. Добавить semaphore для concurrency control
2. Добавить параметр `timeout_sec` в API для гибкости

**Файл:** `backend/api/ai.py`

```python
import asyncio

_ollama_semaphore = asyncio.Semaphore(2)  # max 2 concurrent Ollama requests

@router.post("/analyze")
async def ai_analyze(body: AnalyzeRequest, _user: ...):
    timeout = body.timeout_sec or GENERATE_TIMEOUT  # fallback to 60s
    async with _ollama_semaphore:
        try:
            result = await asyncio.wait_for(
                asyncio.to_thread(generate, ...),
                timeout=timeout
            )
        except asyncio.TimeoutError:
            raise HTTPException(status_code=408, detail="Request timeout")
    ...
```

**Замечание:** Для autolabel с 238 классами 60 секунд может быть необходимо. Не уменьшать default timeout без тестов.

**Smoke test:**
```bash
# 2 concurrent requests → второй должен ждать
curl -X POST http://localhost:8000/api/ai/analyze -d '{"prompt":"test"}' &
curl -X POST http://localhost:8000/api/ai/analyze -d '{"prompt":"test"}' &
wait
```

---

### P0-4: Mutual exclusion через ModelMutex — 10ч 🔴 +4ч

**Проблема:** SAM3↔Seg mutual exclusion работает, но Seg↔Detect НЕТ. YOLO-detect + YOLO-seg могут быть одновременно в VRAM.

**Решение:** Создать отдельный сервис `model_mutex.py` с threading.Lock и глобальным state.

**Файл:** `backend/services/model_mutex.py` (НОВЫЙ)

#### Шаг 1: Базовая реализация mutex (4ч)
```python
import threading
from typing import Literal

_model_lock = threading.Lock()
_active_model: str | None = None  # "yolo_detect" | "yolo_seg" | "sam3"

def acquire_model(model_name: str) -> bool:
    """
    Acquire exclusive access to a model type.
    Unloads all other models before returning.
    Returns True if successfully acquired.
    """
    with _model_lock:
        global _active_model
        
        # Already active — no-op
        if _active_model == model_name:
            return True
        
        # Unload current model
        if _active_model == "yolo_detect":
            try:
                from services.yolo_engine import get_yolo_engine
                get_yolo_engine().unload_model()
                print(f"[ModelMutex] Unloaded YOLO-detect")
            except Exception as e:
                print(f"[ModelMutex] Warning: failed to unload YOLO-detect: {e}")
        
        elif _active_model == "yolo_seg":
            try:
                from services.segmentation_engine import get_seg_engine
                get_seg_engine().unload_model()
                print(f"[ModelMutex] Unloaded YOLO-seg")
            except Exception as e:
                print(f"[ModelMutex] Warning: failed to unload YOLO-seg: {e}")
        
        elif _active_model == "sam3":
            try:
                from services.sam3_engine import get_sam3_engine
                get_sam3_engine().unload_model()
                print(f"[ModelMutex] Unloaded SAM3")
            except Exception as e:
                print(f"[ModelMutex] Warning: failed to unload SAM3: {e}")
        
        _active_model = model_name
        return True

def release_model(model_name: str | None = None) -> None:
    """Release currently active model."""
    with _model_lock:
        global _active_model
        if model_name and _active_model == model_name:
            _active_model = None
        elif not model_name:
            _active_model = None

def get_active_model() -> str | None:
    """Get currently active model type."""
    with _model_lock:
        return _active_model
```

#### Шаг 2: Интеграция в yolo_engine (2ч)
```python
# В yolo_engine.py, метод force_load():
def force_load(self, path: Path | None = None) -> Path:
    # Acquire mutex
    from services.model_mutex import acquire_model
    acquire_model("yolo_detect")
    
    # ... existing load logic ...
```

#### Шаг 3: Интеграция в segmentation_engine (2ч)
```python
# В segmentation_engine.py, метод load_model():
def load_model(self, name: str | None = None) -> Path:
    # Acquire mutex
    from services.model_mutex import acquire_model
    acquire_model("yolo_seg")
    
    # ... existing load logic ...
```

#### Шаг 4: Интеграция в sam3_engine (2ч)
```python
# В sam3_engine.py, метод load_model():
def load_model(self, name: str | None = None) -> Path:
    # Acquire mutex
    from services.model_mutex import acquire_model
    acquire_model("sam3")
    
    # ... existing load logic ...
```

**Smoke test:**
```bash
python -c "
from services.yolo_engine import get_yolo_engine
from services.segmentation_engine import get_seg_engine
from services.model_mutex import get_active_model

yolo = get_yolo_engine()
seg = get_seg_engine()

yolo.force_load(yolo.model_path)
print(f'After YOLO load: {get_active_model()}')  # yolo_detect

seg.load_model()
print(f'After Seg load: {get_active_model()}')  # yolo_seg
print(f'YOLO mode: {yolo.mode}')  # offline

assert yolo.mode == 'offline' or yolo.model is None
"
```

---

### P0-5: LRU cache canonical_label — 2ч ✅

**Файл:** `backend/services/classes.py:347-353`

```python
import functools

@functools.lru_cache(maxsize=512)
def canonical_label(name: str) -> str:
    get_class_catalog()
    raw = (name or "").strip().lower().replace("_", " ")
    if _alias_map and raw in _alias_map:
        return _alias_map[raw]
    return _LABEL_ALIASES.get(raw, (name or "").strip())

# Добавить в invalidate_class_cache():
def invalidate_class_cache() -> None:
    global _alias_map
    _alias_map = None
    canonical_label.cache_clear()  # <-- Новая строка
```

**Smoke test:**
```bash
python -c "
from services.classes import canonical_label; import time
t = time.time(); [canonical_label('tank') for _ in range(1000)]
print(f'1000 calls: {time.time()-t:.3f}s (expected <0.01s)')
"
```

---

### P0-6: Pin depth_anything_3 version — 1ч ✅

**Файл:** `backend/requirements.txt`

```txt
depth_anything_3>=1.0.0,<2.0.0
```

**Smoke test:**
```bash
python -c "import subprocess; print(subprocess.run(['pip','show','depth_anything_3'], capture_output=True, text=True).stdout)"
```

---

## P1 — Высокие (Неделя 2, 25 часов)

### P1-7: SQLite checkpoint batch segmentation — 12ч ⚠️ +4ч

**Проблема:** `_state` в памяти, при краше прогресс теряется. Нет resume.

**Файл:** `backend/services/batch_segmentation.py`, `backend/services/db.py`

#### Шаг 1: Таблица batch_seg_jobs (через migration)
- Добавить в `migrate_db()` (см. новая задача выше, version 2)

#### Шаг 2: Функции checkpoint (3ч)
```python
# В db.py:
def save_batch_seg_checkpoint(task_id: str, state: dict) -> None:
    conn = _connect()
    try:
        conn.execute("""
            INSERT OR REPLACE INTO batch_seg_jobs (
                task_id, video_path, frame_step, confidence,
                status, last_frame_idx, total_frames,
                results_json, updated_at
            ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, strftime('%s', 'now'))
        """, (
            task_id,
            state["video_path"],
            state["frame_step"],
            state["confidence"],
            state["status"],  # running|done|error|aborted
            state["last_frame_idx"],
            state["total_frames"],
            json.dumps(state["results"])
        ))
        conn.commit()
    finally:
        conn.close()

def load_batch_seg_checkpoint(task_id: str) -> dict | None:
    conn = _connect()
    try:
        row = conn.execute(
            "SELECT * FROM batch_seg_jobs WHERE task_id = ?",
            (task_id,)
        ).fetchone()
        if not row:
            return None
        return {
            "task_id": row["task_id"],
            "video_path": row["video_path"],
            "frame_step": row["frame_step"],
            "confidence": row["confidence"],
            "status": row["status"],
            "last_frame_idx": row["last_frame_idx"],
            "total_frames": row["total_frames"],
            "results": json.loads(row["results_json"]),
            "created_at": row["created_at"],
            "updated_at": row["updated_at"],
        }
    finally:
        conn.close()
```

#### Шаг 3: Integration в batch_segmentation (5ч)
```python
# В _run() метода batch_segmentation.py:
def _run(self) -> None:
    # Load checkpoint if exists
    checkpoint = load_batch_seg_checkpoint(self.task_id)
    if checkpoint and checkpoint["status"] == "running":
        print(f"[BatchSeg] Resuming from frame {checkpoint['last_frame_idx']}")
        start_frame = checkpoint["last_frame_idx"]
        results = checkpoint["results"]
    else:
        start_frame = 0
        results = []
    
    for frame_idx in range(start_frame, total_frames):
        # ... process frame ...
        
        # Checkpoint каждые 10 кадров ИЛИ каждые 30 секунд
        if frame_idx % 10 == 0 or (time.time() - last_checkpoint > 30):
            save_batch_seg_checkpoint(self.task_id, {
                "video_path": self.video_path,
                "frame_step": self.frame_step,
                "confidence": self.confidence,
                "status": "running",
                "last_frame_idx": frame_idx,
                "total_frames": total_frames,
                "results": results
            })
```

**Smoke test:**
```bash
# 1. Запустить batch seg
curl -X POST http://localhost:8000/api/batch-segment/start -d '{"video":"..."}'

# 2. Убить процесс через 5 минут
# 3. Перезапустить — должен resume с last_frame_idx
# 4. Проверить, что прогресс не потерян
```

---

### P1-8: GPU memory monitoring — 8ч ⚠️ +2ч

**Файл:** `backend/services/gpu_monitor.py` (НОВЫЙ), `backend/api/` (endpoint)

#### Шаг 1: GPUMonitor class (3ч)
```python
# backend/services/gpu_monitor.py:
import torch
import time
from typing import Any

class GPUMonitor:
    def __init__(self):
        self._samples: list[dict[str, Any]] = []
        self._max_samples = 3600  # 1 hour at 6s interval
    
    def sample(self) -> dict[str, Any]:
        if not torch.cuda.is_available():
            return {"available": False}
        
        free, total = torch.cuda.mem_get_info()
        used = total - free
        return {
            "available": True,
            "free_mb": free // (1024**2),
            "total_mb": total // (1024**2),
            "used_mb": used // (1024**2),
            "usage_pct": round(used / total * 100, 1) if total > 0 else 0,
            "timestamp": time.time()
        }
    
    def get_samples(self, n: int = 60) -> list[dict[str, Any]]:
        return self._samples[-n:]
    
    def record_sample(self) -> None:
        sample = self.sample()
        self._samples.append(sample)
        # Trim old samples
        if len(self._samples) > self._max_samples:
            self._samples = self._samples[-self._max_samples:]

# Singleton instance
_gpu_monitor = GPUMonitor()

def get_gpu_monitor() -> GPUMonitor:
    return _gpu_monitor
```

#### Шаг 2: Integration в status_snapshot (3ч)
```python
# В yolo_engine.py:
def status_snapshot(self) -> dict:
    snapshot = {
        "mode": self.mode,
        "device": str(self._device),
        "degraded": self._degraded,
        # ... existing fields ...
    }
    
    # Add GPU metrics
    try:
        from services.gpu_monitor import get_gpu_monitor
        monitor = get_gpu_monitor()
        snapshot["gpu"] = monitor.sample()
        monitor.record_sample()
    except Exception:
        pass
    
    return snapshot
```

#### Шаг 3: API endpoint (2ч)
```python
# backend/api/monitor.py:
@router.get("/gpu/status")
async def gpu_status():
    from services.gpu_monitor import get_gpu_monitor
    monitor = get_gpu_monitor()
    return {
        "current": monitor.sample(),
        "history": monitor.get_samples(60)
    }
```

**Smoke test:**
```bash
python -c "from services.gpu_monitor import GPUMonitor; print(GPUMonitor().sample())"
curl http://localhost:8000/api/gpu/status
```

---

### P1-9: Prompt sanitization — 2ч ✅

**Файл:** `backend/api/ai.py:139-146`

```python
def _sanitize_prompt(prompt: str) -> str:
    """Escape HTML/special chars to prevent prompt injection."""
    return (
        prompt.replace("&", "&amp;")
        .replace("<", "&lt;")
        .replace(">", "&gt;")
        .replace('"', "&quot;")
        .replace("'", "&#x27;")
    )

# В ai_analyze:
prompt = (body.prompt or "").strip()
if prompt:
    prompt = _sanitize_prompt(prompt)
    full_prompt = f"{_VISION_PROMPT_PREFIX}\n\nUser prompt: {prompt}"
```

**Smoke test:**
```bash
curl -X POST http://localhost:8000/api/ai/analyze \
  -d '{"prompt":"<script>alert(1)</script>test"}'
# Response должен содержать escaped HTML
```

---

### P1-10: Aliases в YAML — 4ч ✅

**Файл:** `backend/services/classes.py`, `military_classes.yaml`

**Шаги:**
1. Добавить в `military_classes.yaml`:
```yaml
label_aliases:
  car: military truck
  truck: military truck
  bus: military truck
  airplane: military aircraft
  helicopter: military helicopter
  motorcycle: military jeep
  wreck: armored vehicle
  crater: shell crater with scorched earth
  # ... все 40+ маппингов

alias_confidence_thresholds:
  car: 0.7
  truck: 0.7
  # ... (опционально, default 0.5)
```

2. Заменить `_LABEL_ALIASES` в `classes.py` на загрузку из YAML
3. Добавить `confidence_threshold` для каждого alias (по умолчанию 0.5)

**Smoke test:**
```bash
python -c "
from services.classes import canonical_label
print(canonical_label('car'))  # military truck
"
```

---

## 🧪 Integration Tests (добавлено после аудита)

### tests/test_mutual_exclusion.py
```python
def test_yolo_seg_exclusion():
    from services.yolo_engine import get_yolo_engine
    from services.segmentation_engine import get_seg_engine
    from services.model_mutex import get_active_model
    
    yolo = get_yolo_engine()
    seg = get_seg_engine()
    
    # Load YOLO-detect
    yolo.force_load(yolo.model_path)
    assert yolo.mode == "ready"
    assert get_active_model() == "yolo_detect"
    
    # Load YOLO-seg → должен выгрузить YOLO-detect
    seg.load_model()
    assert yolo.mode == "offline" or yolo.model is None
    assert get_active_model() == "yolo_seg"
```

### tests/test_oom_handling.py
```python
def test_oom_cuda_fallback(monkeypatch):
    from services.yolo_engine import YoloEngine
    
    # Mock CUDA OOM on first load
    def mock_yolo(path, device="cuda"):
        if device == "cuda":
            raise RuntimeError("CUDA out of memory")
        return MagicMock()  # Success on CPU
    
    monkeypatch.setattr("services.yolo_engine.YOLO", mock_yolo)
    
    engine = YoloEngine()
    engine.force_load(engine.model_path)
    
    assert engine._degraded == True
    assert str(engine._device) == "cpu"
```

### tests/test_checkpoint_resume.py
```python
def test_batch_seg_resume():
    from services.db import save_batch_seg_checkpoint, load_batch_seg_checkpoint
    
    # Save checkpoint
    save_batch_seg_checkpoint("test-task", {
        "video_path": "/test/video.mp4",
        "frame_step": 30,
        "confidence": 0.25,
        "status": "running",
        "last_frame_idx": 150,
        "total_frames": 1000,
        "results": [{"frame": 150, "objects": []}]
    })
    
    # Load and verify
    checkpoint = load_batch_seg_checkpoint("test-task")
    assert checkpoint is not None
    assert checkpoint["last_frame_idx"] == 150
    assert checkpoint["status"] == "running"
```

---

## 📦 Deployment Checklist

- [ ] `migrate_db()` вызывается при FastAPI startup
- [ ] `depth_anything_3>=1.0.0,<2.0.0` в `requirements.txt`
- [ ] `.env` содержит `SCHEMA_VERSION` (опционально, для отладки)
- [ ] Logs для mutual exclusion записываются в отдельный файл
- [ ] GPU monitoring interval configurable (default 6s)
- [ ] Batch seg checkpoint interval configurable (default: 10 frames OR 30s)

---

## 🔄 Git Workflow

```bash
# Начать
git checkout -b fix/ml-audit-fixes

# Порядок коммитов (рекомендуемый):
git add -A && git commit -m "feat(db): add schema versioning and migrations"
git add -A && git commit -m "fix(ml): P0-3 rate limiting Ollama API"
git add -A && git commit -m "fix(ml): P0-5 LRU cache canonical_label"
git add -A && git commit -m "fix(ml): P0-6 pin depth_anything_3 version"
git add -A && git commit -m "fix(ml): P0-1 dynamic excluded classes"
git add -A && git commit -m "fix(ml): P0-2 OOM graceful degradation"
git add -A && git commit -m "fix(ml): P0-4 model mutex for VRAM exclusion"
git add -A && git commit -m "fix(ml): P1-9 prompt sanitization"
git add -A && git commit -m "fix(ml): P1-10 aliases in YAML"
git add -A && git commit -m "feat(monitoring): P1-8 GPU memory monitoring"
git add -A && git commit -m "feat(batch): P1-7 SQLite checkpoint + resume"

# Добавить tests
git add -A && git commit -m "test: add integration tests for mutual exclusion, OOM, checkpoint"

# Push
git push origin fix/ml-audit-fixes
```

---

## ✅ Acceptance Criteria (все задачи)

### P0
- [ ] P0-1: `_COCO_DROP` удалён, 78 классов в SQLite, API работает, cache invalidation работает
- [ ] P0-2: `torch.cuda.empty_cache()` вызывается при OOM, fallback на CPU работает
- [ ] P0-3: `asyncio.Semaphore(2)` на Ollama API, timeout configurable
- [ ] P0-4: `model_mutex.py` создан, mutual exclusion работает для всех 3 моделей
- [ ] P0-5: `@lru_cache(maxsize=512)` на canonical_label, cache_clear при invalidate
- [ ] P0-6: `depth_anything_3>=1.0.0,<2.0.0` в requirements.txt

### P1
- [ ] P1-7: SQLite checkpoint каждые 10 кадров/30с, resume после crash работает
- [ ] P1-8: `/api/gpu/status` возвращает VRAM metrics, history доступен
- [ ] P1-9: Prompt sanitization applied, HTML escaping работает
- [ ] P1-10: Aliases загружаются из YAML, confidence_threshold поддерживается

### DB Schema
- [ ] Таблица `settings` с `schema_version` существует
- [ ] `migrate_db()` обрабатывает incremental upgrades
- [ ] Seed данных использует `INSERT OR IGNORE`

### Tests
- [ ] `test_mutual_exclusion.py` проходит
- [ ] `test_oom_handling.py` проходит
- [ ] `test_checkpoint_resume.py` проходит

---

## 📊 Итоговая оценка аудита

| Критерий | До аудита | После аудита | Комментарий |
|----------|-----------|--------------|-------------|
| Полнота | 7/10 | **8.5/10** | Добавлен model_mutex, migration system |
| Реалистичность оценок | 6/10 | **8/10** | Увеличены с 36ч до 49ч |
| Тестируемость | 5/10 | **8/10** | Добавлены integration tests |
| Безопасность | 8/10 | **8.5/10** | Prompt sanitization + DB escaping |
| Масштабируемость | 6/10 | **8/10** | Migration system + schema versioning |
| **Средняя** | **6.4/10** | **8.2/10** | **✅ План готов к реализации** |

---

## 🎯 Рекомендации по приоритизации

1. **Начать с P0-3, P0-5, P0-6** (наименьший риск, 5ч)
2. **Затем P0-1, P0-2** (средний риск, 12ч)
3. **Затем P0-4** (наибольший риск, 10ч)
4. **P1-9, P1-10** (низкий риск, 6ч)
5. **P1-8, P1-7** (средний риск, 20ч)

**Минимальный viable P0:** P0-3 + P0-5 + P0-6 + P0-1 (13ч)  
**Полный P0:** Все 6 задач (24ч)  
**Полный план:** P0 + P1 (49ч)

---

*План обновлён на основе аудита от 2026-09-18. Готов к реализации.*

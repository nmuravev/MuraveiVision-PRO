# Руководство аналитика

Аналитик работает с результатами детекции: проверяет, исправляет, собирает отчёты и экспортирует датасеты. Роль не требует PIN инженера — достаточно входа оператора/аналитика. Фокус — качество и достоверность данных.

## Вход

1. `Запустить.bat` (или `http://127.0.0.1:8000`).
2. PIN аналитика/оператора (по умолчанию `1234567`; сменить после первого развёртывания).
3. Открыть вкладку **Монтаж**.

## Просмотр и проверка детекций

1. MediaPool → перетащить видео во Viewer.
2. Inspector показывает список детекций **только текущего ролика** (scoped-фильтр по `source_video`). Сверху — `N/N` текущего ролика.
3. Сверить оверлеи YOLO с кадром: бейдж модели / ms / device во Viewer. **YOLO вкл/выкл** — покадровый WS (не пакетный скан). **«Сканировать»** — batch по ролику или I–O. Если `mode != ready` — см. [ENGINEER_GUIDE.md](ENGINEER_GUIDE.md) («Если ничего не детектит»). В узкой колонке mosaic (~&lt;980px) сегментация, векторы, Было/Стало, REC и I–O — в меню **«Ещё»** (⋯).
4. **HUD (авто):** на роликах с OSD включено исключение полос (архив по умолчанию). В левом верхнем углу Viewer — кнопка с иконкой глаза (по умолчанию свёрнута, не перекрывает обзор); клик раскрывает бейдж «HUD исключён: верх/низ …%» с действиями «правка» / «сброс», повторный клик сворачивает. Live — только если инженер включил Live HUD. Центральные оверлеи — ручные зоны (`PUT /api/hud/zones`).
5. Переключение вкладок сохраняет scoped-N (soft-delete переживает переключения).

## Исправление и правка

- **Класс:** в Inspector сменить класс детекции; в режиме рисования во Viewer — выбрать класс в `<select>` перед рамкой.
- **Заметки:** `user_notes` (дебаунс), сохраняются в БД.
- **Флаг/soft-delete:** пометить ложную детекцию `is_deleted` (не удаляется физически — остаётся для аудита).
- **Bulk «Очистить»:** мягко удалить все детекции текущего ролика.
- **Ollama autolabel (опц.):** в Inspector «Предложить класс через Ollama» → предложение с confidence/reason → «Принять» патчит детекцию, «Отклонить» очищает предложение.
- **Похожие:** зафиксированная детекция → «Найти похожие». Клик по результату открывает тот ролик и перематывает к кадру. Метод: CLIP (если веса уже на машине) или гистограмма.

## Сегментация (архив)

Только Архив, не Live. Тумблер Viewer **Сегментация**:

1. Поставить ролик на паузу.
2. Если подпись «загрузите модель» — кнопка **Загрузить** (или Система → «Сегментация» для выбора nano/small).
3. **Сегментировать кадр** — полупрозрачные полигоны на кадре. Не bbox детекции, не пишется в обучение.
4. Смена ролика внутри SEG не выгружает модель. Переключение на **Детекция** или **Выгрузить** освобождает VRAM.
5. **Пакетная сегментация (P3.13.2):** при loaded-модели — **Batch сегментация** → шаг кадров (по умолчанию 30) и confidence → Старт → прогресс. По завершении: «Обработано N кадров, найдено M масок»; клик по кадру — seek + показ масок. Результаты не пишутся в БД. Если был загружен SAM3 — он выгрузится (сообщение в модалке); сам не вернётся.
6. **SAM3 refine (P3.13.3a):** при наличии `sam3.pt` — **Загрузить SAM3** (YOLO-seg выгрузится) → **Точка** (ЛКМ объект, Shift+ЛКМ фон) или **SAM из детекции** (нужна активная детекция) → маски на том же SVG. Пауза обязательна. Не пишет в обучение.
7. **SAM3 propagate (P3.13.3b):** после seed — **Пропагировать** → вперёд ≤30 кадров; опционально checkbox «В SQLite». Клик по кадру — seek + маска. `seg_masks` ≠ detections.
8. **SAM3 text (P3.13.3c):** поле промпта (`trench / окоп; person; vehicle`, до 3 через `;`) → **По тексту** на паузе. Seed для propagate может быть текстовым.
9. **Live «Кадр SAM»:** на live stream — **Загрузить SAM3** → текст → **Кадр SAM** (один JPEG). Detect-оверлеи продолжают идти. Overlay сбрасывается по **Сброс SAM** или при play. Continuous Live SAM нет.

Нет `yolo26n-seg.pt` — сегментация YOLO недоступна, YOLO-детекция работает как раньше. Нет `sam3.pt` — кнопки SAM скрыты.

## Active Learning

Вкладка **Обучение** → очередь Active Learning:
1. Low-confidence детекции собраны автоматически (`POST /api/active-learning/collect`).
2. Подтвердить полезные (`accepted`) — остаются в датасете.
3. Отметить ложные (`rejected`) — исключаются soft-delete.
4. Решение фиксирует `POST /api/active-learning/{id}/decision`.

## Кропы и галерея

- «Зафиксировать кадр» или **«Сканировать»** → кропы в BattleGallery.
- Галерея — уникальные кропы текущего ролика (дубли схлопываются, до 96 групп), **«Очистить ролик»** удаляет все детекции ролика.
- Кропы — материал для дообучения и отчётов.

## Отчёты и экспорт

- **HTML-отчёт:** TopBar → автономный HTML-файл (открывается без сервера).
- **PDF/geo-карта:** вкладка отчётов, GPS-точки по SRT/CSV телеметрии (если есть).
- **Экспорт датасета YOLO:** для внешнего дообучения (формат YOLO).
- **Diagnostic ZIP:** если нужна помощь инженера — собрать Support-архив (роль инженера, см. [ENGINEER_GUIDE.md](ENGINEER_GUIDE.md)).

## Compare Sync (сверка двух ракурсов)

1. Layout preset «2 Viewer».
2. Перетащить второй ролик во Viewer-2.
3. «Sync» — синхронизация воспроизведения, drift-correction 500мс.
4. Сверить детекции по таймстампу между ракурсами.

## Change Detection (P3.15, Compare Sync)

1. Включить **Было/Стало** на viewer-1, загрузить ролики в viewer-1 (было) и viewer-2 (стало).
2. При необходимости **Sync** — drift-correction 500 ms; окно анализа тогда ±0.5 с.
3. **Синхронизация времени (P3.15.2):** на viewer-1 **«Синхронизировать»** → Auto / GPS Треки / Детекции → список сегментов → клик по сегменту (оба Viewer seek на парные кадры). Если треков нет — fallback на детекции или ручной выбор.
4. Поставить оба ролика на **паузу** на нужных кадрах (или после клика по сегменту).
5. На viewer-1 нажать **«Анализ изменений»** — GPS-matching сохранённых детекций (пороги: stable <3 m, moved 3–10 m).
6. Результат: Inspector «Изменения (Compare)» + цветные bbox (зелёный новые / красный исчезли / жёлтый перемещены).
7. Без Sync окно ±2 с; при слабом GPS — ORB/diff fallback (см. `message` в ответе API).
8. **Экспорт отчёта:** в Inspector «Изменения (Compare)» — **Экспорт HTML** (таблицы + карта Leaflet при наличии GPS) или **Экспорт KML** (Google Earth / QGIS; нужна GPS).
9. **Тепловая карта (P3.15.4):** если анализ шёл через ORB/diff (`image_diff.heatmap_b64`), на viewer-1 появляется **«Теплокарта»**. Вкл/выкл — полупрозрачный JET-overlay под цветными bbox на обоих Viewer (координаты «было»). GPS-only без image-пути — кнопки нет.
10. **Пакетный CD (P3.15.5):** кнопка **«Пакетный CD»** → stride / max_pairs / opt-in ORB → Start. После done — список пар; клик → seek обоих Viewer + Inspector. **Экспорт HTML** — сводка уникальных ID по всем парам.

## Гео 3D (кратко)

После «Построить 3D» на canvas — **sparse COLMAP** (облако точек), не фотореализм. В статус-баре: **`sparse COLMAP · нужен train для splat`**. Во время COLMAP/обучения — модалка шагов; в idle — карточка «→ Balanced». Фотореализм = Gaussian splat после Balanced/High (`model.ply`). Тестировать UI через Vite `:3000` (после снятия debug-логов — **Ctrl+Shift+R**, иначе возможны CSP-ошибки на устаревший `:7307`). Подробнее: [OPERATOR_GUIDE.md](OPERATOR_GUIDE.md).

## Достоверность (важно)

- Response Validator отбрасывает аномальные детекции (bbox/conf/area/class_id) — см. `logs/validator_rejections.jsonl`. Если аналитик видит систематические отбросы — сообщить инженеру.
- Правила/alerts (вкладка AI-анализ) — cooldown 3с, локальный сигнал при срабатывании, авто-фиксация в галерее.
- SAHI (если включён инженером) — для мелких объектов на 4K-кадрах; не меняет формат детекций.
